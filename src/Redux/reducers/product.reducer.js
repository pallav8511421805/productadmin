import * as Actiontypes from '../actions/Actiontype';

const intval = {
    isload: false,
    productdata: [],
    error: ''
}
export const productreducer = (state = intval ,action) => {
    switch(action.type){
        case Actiontypes
    }
}